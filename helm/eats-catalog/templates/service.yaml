apiVersion: v1
kind: Service
metadata:
  name: eats-catalog
spec:
  type: {{ .Values.service.type }}
  selector:
    app: eats-catalog
  ports:
    - name: http
      port: {{ .Values.service.port }}
      targetPort: 3000
