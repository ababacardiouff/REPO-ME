{{- if .Values.seed.enabled }}
apiVersion: batch/v1
kind: CronJob
metadata:
  name: moderation-seed-images
spec:
  schedule: {{ .Values.seed.schedule.images | quote }}
  jobTemplate:
    spec:
      template:
        spec:
          restartPolicy: Never
          containers:
            - name: moderation-seed-images
              image: "{{ .Values.image.repository }}:{{ .Values.image.tag }}"
              command: ["npx", "ts-node", "scripts/seed_moderation_images.ts"]
              env:
                - name: DATABASE_URL
                  valueFrom:
                    secretKeyRef:
                      name: moderation-secrets
                      key: DATABASE_URL
{{- end }}
