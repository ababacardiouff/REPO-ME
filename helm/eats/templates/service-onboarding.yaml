apiVersion: v1
kind: Service
metadata:
  name: eats-onboarding
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: 3000
      name: http
  selector:
    app: eats-onboarding
