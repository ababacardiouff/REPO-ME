apiVersion: v1
kind: Service
metadata:
  name: eats-agents
spec:
  type: {{ .Values.service.type }}
  selector:
    app: eats-agents
  ports:
    - port: {{ .Values.service.port }}
      targetPort: {{ .Values.service.targetPort }}
      protocol: TCP
      name: http
